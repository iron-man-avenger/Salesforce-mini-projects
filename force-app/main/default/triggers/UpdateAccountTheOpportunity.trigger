trigger UpdateAccountTheOpportunity on Account (after update) {
   /* List<Account> updatedAccounts = Trigger.new;
    Map<Id, Account> oldAccountMap = Trigger.oldMap;
    
    // Call the updateOpportunitySimilarValue method in the AccountTriggerHelper class
    AccountTriggerHelper.updateOpportunitySimilarValue(updatedAccounts, oldAccountMap);
*/
}