public with sharing class OpportunityHepler {
    public static void changeAmountThenchangeDiscount(List<Opportunity>  theList)  {
        // SecondBatch obj = new SecondBatch();
        // ID JobId = Database.ExecuteBatch(obj, 200);
        }
}